<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>User Profile</title>
    <style>
      /* Reset some default styles */
      * {
          margin: 0;
          padding: 0;
          box-sizing: border-box;
      }

      /* Style for the body */
      body {
          font-family: Arial, sans-serif;
          background-color: #f0f0f0;
          text-align: center;
      }

      /* Style for the profile container */
      .profile {
          background-color: #fff;
          border-radius: 10px;
          box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
          max-width: 400px;
          margin: 50px auto;
          padding: 20px;
      }

      /* Style for the profile picture */
      .profile img {
          width: 150px;
          height: 150px;
          border-radius: 50%;
          object-fit: cover;
          margin: 0 auto 20px;
      }

      /* Style for the user's name */
      .profile h1 {
          font-size: 24px;
          color: #333;
          margin-bottom: 10px;
      }

      /* Style for user details */
      .profile p {
          font-size: 16px;
          color: #666;
          margin-bottom: 10px;
      }

      /* Style for the edit profile button */
      .profile button {
          background-color: #007bff;
          color: #fff;
          border: none;
          border-radius: 5px;
          padding: 10px 20px;
          font-size: 18px;
          cursor: pointer;
          transition: background-color 0.3s;
      }

      .profile button:hover {
          background-color: #0056b3;
      }
    </style>
  </head>
  <body>
    <div class="profile">
      <h2>My Account</h2><br>
      <%= image_tag @user.profile_image, size:122 if @user.profile_image.attached? %> 
      <p>Role: <%= @user.role%></p>
      <h1><%= @user.username %></h1>
      <p>Email: <%= @user.email%></p>
      <p>Location: New York, USA</p>
      <p>Joined: <%= @user.created_at%></p>
      <button><%= link_to "Edit Profile", edit_user_path(@user.id), class: "btn btn-primary", style: "text-decoration: none; color: white;" %></button>
      <button><%= link_to "Back", landings_path, style:"text-decoration: none; color:white;" %></button>
    </div>
  </body>
</html>
